---
interface Props {
	type?: "info" | "warning" | "error" | "success";
	title?: string;
}

const { type = "info", title } = Astro.props;

const typeStyles = {
	info: {
		border: "border-ocean-blue",
		bg: "bg-ocean-blue/10",
		icon: "ℹ",
		titleColor: "text-ocean-blue",
	},
	warning: {
		border: "border-yellow-400",
		bg: "bg-yellow-400/10",
		icon: "⚠",
		titleColor: "text-yellow-400",
	},
	error: {
		border: "border-red-400",
		bg: "bg-red-400/10",
		icon: "✕",
		titleColor: "text-red-400",
	},
	success: {
		border: "border-ocean-green",
		bg: "bg-ocean-green/10",
		icon: "✓",
		titleColor: "text-ocean-green",
	},
};

const style = typeStyles[type];
---

<div
    class={`my-6 border-l-4 ${style.border} ${style.bg} p-4 rounded-r-lg font-mono`}
>
    {
        title && (
            <div
                class={`flex items-center gap-2 mb-2 font-bold ${style.titleColor}`}
            >
                <span class="text-lg">{style.icon}</span>
                <span>{title}</span>
            </div>
        )
    }
    <div class="text-ocean-text/90 leading-relaxed">
        <slot />
    </div>
</div>
